#usda 1.0
(
    doc ="""Generated from node description file OgnSdPostRenderVarToHost.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_syntheticdata_SdPostRenderVarToHost" (
        docs="""Expose a host renderVar from the input device renderVar."""
    )
    {
        token node:type = "omni.syntheticdata.SdPostRenderVarToHost"
        int node:typeVersion = 1

        # 4 attributes
        custom uint64 inputs:gpu = 0 (
            docs="""Pointer to shared context containing gpu foundations"""
        )
        custom token inputs:renderVar = "" (
            docs="""Name of the device renderVar to expose on the host"""
        )
        custom string inputs:renderVarHostSuffix = "host" (
            docs="""Suffix appended to the renderVar name"""
        )
        custom uint64 inputs:rp = 0 (
            docs="""Pointer to render product for this view"""
        )

        # 1 attribute
        custom token outputs:renderVar (
            docs="""Name of the resulting renderVar on the host"""
        )
    }
}
