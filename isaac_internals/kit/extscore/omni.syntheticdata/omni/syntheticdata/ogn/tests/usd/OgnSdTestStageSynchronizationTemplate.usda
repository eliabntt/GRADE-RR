#usda 1.0
(
    doc ="""Generated from node description file OgnSdTestStageSynchronization.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_syntheticdata_SdTestStageSynchronization" (
        docs="""Synthetic Data node to test the pipeline stage synchronization"""
    )
    {
        token node:type = "omni.syntheticdata.SdTestStageSynchronization"
        int node:typeVersion = 1

        # 9 attributes
        custom uint inputs:exec (
            docs="""OnDemand connection : trigger"""
        )
        custom uint64 inputs:gpu = 0 (
            docs="""PostRender connection : pointer to shared context containing gpu foundations"""
        )
        custom uint inputs:randomMaxProcessingTimeUs = 0 (
            docs="""Maximum number of micro-seconds to randomly (uniformely) wait for in order to simulate varying workload"""
        )
        custom uint inputs:randomSeed = 0 (
            docs="""Random seed for the randomization"""
        )
        custom uint64 inputs:renderResults = 0 (
            docs="""OnDemand connection : pointer to render product results"""
        )
        custom uint64 inputs:rp = 0 (
            docs="""PostRender connection : pointer to render product for this view"""
        )
        custom uint64 inputs:swhFrameNumber = 0 (
            docs="""Fabric frame number"""
        )
        custom token inputs:tag = "" (
            docs="""A tag to identify the node"""
        )
        custom bool inputs:traceError = false (
            docs="""If true print an error message when the frame numbers are out-of-sync"""
        )

        # 3 attributes
        custom uint outputs:exec (
            docs="""OnDemand connection : trigger"""
        )
        custom uint64 outputs:fabricSWHFrameNumber (
            docs="""Fabric frame number from the fabric"""
        )
        custom uint64 outputs:swhFrameNumber (
            docs="""Fabric frame number"""
        )
    }
}
